#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n;
    cin >> n;
    // Interactive: use "? k a1 a2 ... ak" to query, "! edges" to answer
    // cout << "? " << k << " " << ...  << endl;
    // string response; cin >> response;
    // cout << "!" << endl;
    // for each edge: cout << u << " " << v << "\n";
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}
