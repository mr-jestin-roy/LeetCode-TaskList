#include <bits/stdc++.h>
using namespace std;

void solve()
{
    long long n, d;
    int m;
    cin >> n >> m >> d;
    vector<long long> p(m), r(m);
    for (int i = 0; i < m; i++)
        cin >> p[i] >> r[i];
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--)
        solve();
    return 0;
}
